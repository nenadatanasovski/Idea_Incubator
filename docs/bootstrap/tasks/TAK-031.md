# TAK-031: DependencyManager Unit Tests

---

## Metadata

| Field | Value |
|-------|-------|
| **Phase** | 8 - Unit Tests |
| **Depends On** | TAK-013 |
| **Blocks** | TAK-038 (E2E Tests) |
| **Priority** | P1 |
| **Owner** | Build Agent |

---

## Summary

Create unit tests for the DependencyManager service covering dependency addition, circular dependency detection, auto-resolution, and critical path calculation.

---

## Requirements

1. **Test Cases**:
   - Test dependency addition
   - Test circular dependency detection
   - Test auto-resolution
   - Test critical path calculation

---

## Pass Criteria

**PASS** when ALL of the following are true:

| # | Criterion | How to Verify |
|---|-----------|---------------|
| 1 | File exists | `test -f tests/unit/task-agent/dependency-manager.test.ts` returns 0 |
| 2 | Has cycle tests | `grep -q "cycle\|circular" tests/unit/task-agent/dependency-manager.test.ts` returns 0 |
| 3 | Has critical path tests | `grep -q "critical.*path" tests/unit/task-agent/dependency-manager.test.ts` returns 0 |
| 4 | Tests pass | `npm test -- --grep "dependency-manager"` passes |

**FAIL** if any criterion is not met.

---

## Output Files

```
tests/unit/task-agent/
└── dependency-manager.test.ts
```
