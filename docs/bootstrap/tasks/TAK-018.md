# TAK-018: Mount Task Agent Routes in API

---

## Metadata

| Field          | Value               |
| -------------- | ------------------- |
| **Phase**      | 4 - API Routes      |
| **Depends On** | TAK-017             |
| **Blocks**     | TAK-032 (API Tests) |
| **Priority**   | P1                  |
| **Owner**      | Build Agent         |

---

## Summary

Import and mount the task-agent routes in server/api.ts at the /api/task-agent path.

---

## Requirements

1. **Update server/api.ts**:
   - Import task-agent routes
   - Mount at /api/task-agent
   - Add to route documentation

---

## Pass Criteria

**PASS** when ALL of the following are true:

| #   | Criterion           | How to Verify                                                               |
| --- | ------------------- | --------------------------------------------------------------------------- |
| 1   | Import exists       | `grep -q "import.*task-agent\|require.*task-agent" server/api.ts` returns 0 |
| 2   | Route mounted       | `grep -q "/api/task-agent\|/task-agent" server/api.ts` returns 0            |
| 3   | TypeScript compiles | `npx tsc --noEmit server/api.ts` returns 0                                  |
| 4   | Route accessible    | `curl -s http://localhost:3001/api/task-agent/tasks` returns 200 or array   |

**FAIL** if any criterion is not met.

---

## Output Files

```
server/
└── api.ts (UPDATE)
```

---

## Validation

```bash
npx tsc --noEmit server/api.ts
curl http://localhost:3001/api/task-agent/tasks
```

---

## Next Steps

After completing: Add 'task' agent type to bot registry (TAK-019).
