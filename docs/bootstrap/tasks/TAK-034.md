# TAK-034: Task Validation Block API Test Case

---

## Metadata

| Field          | Value                  |
| -------------- | ---------------------- |
| **Phase**      | 9 - API Test Cases     |
| **Depends On** | TAK-017                |
| **Blocks**     | TAK-015 (TestExecutor) |
| **Priority**   | P1                     |
| **Owner**      | Build Agent            |

---

## Summary

Define API test case JSON for task validation blocking, testing that tasks without required tests are blocked and ambiguous descriptions trigger warnings.

---

## Requirements

1. **Test Case Structure**:
   - Task without API tests is blocked
   - Task with ambiguous description triggers warning
   - Validation endpoint returns issues

---

## Pass Criteria

**PASS** when ALL of the following are true:

| #   | Criterion         | How to Verify                                                                                            |
| --- | ----------------- | -------------------------------------------------------------------------------------------------------- |
| 1   | File exists       | `test -f tests/api-test-cases/task-validation-block.json` returns 0                                      |
| 2   | Valid JSON        | `python3 -c "import json; json.load(open('tests/api-test-cases/task-validation-block.json'))"` returns 0 |
| 3   | Has blocked check | `grep -q '"blocked"\|validationStatus' tests/api-test-cases/task-validation-block.json` returns 0        |

**FAIL** if any criterion is not met.

---

## Output Files

```
tests/api-test-cases/
└── task-validation-block.json
```
