# TAK-035: Task Dashboard UI Test Case

---

## Metadata

| Field | Value |
|-------|-------|
| **Phase** | 10 - UI Test Cases |
| **Depends On** | TAK-022 |
| **Blocks** | TAK-015 (TestExecutor) |
| **Priority** | P1 |
| **Owner** | Build Agent |

---

## Summary

Define UI test case JSON for Task Agent dashboard, verifying page loads with task stats and blocked tasks section.

---

## Requirements

1. **Test Steps**:
   - Navigate to /task-agent
   - Wait for page load
   - Capture screenshot

2. **Assertions**:
   - Task stats component exists
   - Blocked tasks section exists
   - Page title contains "Task Agent"

---

## Pass Criteria

**PASS** when ALL of the following are true:

| # | Criterion | How to Verify |
|---|-----------|---------------|
| 1 | File exists | `test -f tests/ui-test-cases/task-dashboard.json` returns 0 |
| 2 | Valid JSON | `python3 -c "import json; json.load(open('tests/ui-test-cases/task-dashboard.json'))"` returns 0 |
| 3 | Has startUrl | `grep -q '"startUrl"' tests/ui-test-cases/task-dashboard.json` returns 0 |
| 4 | Has assertions | `grep -q '"assertions"' tests/ui-test-cases/task-dashboard.json` returns 0 |

**FAIL** if any criterion is not met.

---

## Output Files

```
tests/ui-test-cases/
└── task-dashboard.json
```
