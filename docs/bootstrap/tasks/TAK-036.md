# TAK-036: Task Create Flow UI Test Case

---

## Metadata

| Field          | Value                  |
| -------------- | ---------------------- |
| **Phase**      | 10 - UI Test Cases     |
| **Depends On** | TAK-022, TAK-024       |
| **Blocks**     | TAK-015 (TestExecutor) |
| **Priority**   | P1                     |
| **Owner**      | Build Agent            |

---

## Summary

Define UI test case JSON for complete task creation flow through the UI, including form filling and submission.

---

## Requirements

1. **Test Steps**:
   - Click create task button
   - Fill title and description
   - Select category
   - Add acceptance criteria
   - Submit form

2. **Assertions**:
   - New task appears in list
   - Form clears after submit

---

## Pass Criteria

**PASS** when ALL of the following are true:

| #   | Criterion      | How to Verify                                                                                      |
| --- | -------------- | -------------------------------------------------------------------------------------------------- |
| 1   | File exists    | `test -f tests/ui-test-cases/task-create-flow.json` returns 0                                      |
| 2   | Valid JSON     | `python3 -c "import json; json.load(open('tests/ui-test-cases/task-create-flow.json'))"` returns 0 |
| 3   | Has click step | `grep -q '"click"' tests/ui-test-cases/task-create-flow.json` returns 0                            |
| 4   | Has type step  | `grep -q '"type"' tests/ui-test-cases/task-create-flow.json` returns 0                             |

**FAIL** if any criterion is not met.

---

## Output Files

```
tests/ui-test-cases/
└── task-create-flow.json
```
