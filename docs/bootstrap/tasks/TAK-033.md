# TAK-033: Task Create API Test Case

---

## Metadata

| Field          | Value                  |
| -------------- | ---------------------- |
| **Phase**      | 9 - API Test Cases     |
| **Depends On** | TAK-017                |
| **Blocks**     | TAK-015 (TestExecutor) |
| **Priority**   | P1                     |
| **Owner**      | Build Agent            |

---

## Summary

Define API test case JSON for task creation endpoint, testing happy path, validation failures, and duplicate detection.

---

## Requirements

1. **Test Case Structure**:
   - Happy path: valid task creation
   - Validation failure: missing required fields
   - Duplicate detection: similar task exists

---

## Pass Criteria

**PASS** when ALL of the following are true:

| #   | Criterion    | How to Verify                                                                                  |
| --- | ------------ | ---------------------------------------------------------------------------------------------- |
| 1   | File exists  | `test -f tests/api-test-cases/task-create.json` returns 0                                      |
| 2   | Valid JSON   | `python3 -c "import json; json.load(open('tests/api-test-cases/task-create.json'))"` returns 0 |
| 3   | Has method   | `grep -q '"method"' tests/api-test-cases/task-create.json` returns 0                           |
| 4   | Has endpoint | `grep -q '"endpoint"' tests/api-test-cases/task-create.json` returns 0                         |

**FAIL** if any criterion is not met.

---

## Output Files

```
tests/api-test-cases/
└── task-create.json
```
