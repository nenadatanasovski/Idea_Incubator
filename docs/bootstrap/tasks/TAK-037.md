# TAK-037: Task Validation Feedback UI Test Case

---

## Metadata

| Field | Value |
|-------|-------|
| **Phase** | 10 - UI Test Cases |
| **Depends On** | TAK-022, TAK-025 |
| **Blocks** | TAK-015 (TestExecutor) |
| **Priority** | P1 |
| **Owner** | Build Agent |

---

## Summary

Define UI test case JSON for validation error display, testing that validation errors are shown correctly and blocking indicator appears.

---

## Requirements

1. **Test Steps**:
   - Open create task modal
   - Enter short title and vague description
   - Trigger validation
   - Capture screenshot

2. **Assertions**:
   - Validation errors visible
   - Error mentions title issue
   - Blocking indicator shows

---

## Pass Criteria

**PASS** when ALL of the following are true:

| # | Criterion | How to Verify |
|---|-----------|---------------|
| 1 | File exists | `test -f tests/ui-test-cases/task-validation-feedback.json` returns 0 |
| 2 | Valid JSON | `python3 -c "import json; json.load(open('tests/ui-test-cases/task-validation-feedback.json'))"` returns 0 |
| 3 | Has validation error check | `grep -q 'validation.*error\|error.*validation' tests/ui-test-cases/task-validation-feedback.json` returns 0 |

**FAIL** if any criterion is not met.

---

## Output Files

```
tests/ui-test-cases/
└── task-validation-feedback.json
```
