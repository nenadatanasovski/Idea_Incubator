# SIA-002: Build SIA Using Build Agent

---

## Metadata

| Field | Value |
|-------|-------|
| **Phase** | 9 - Self-Improvement Agent |
| **Depends On** | SIA-001, BLD-009 |
| **Blocks** | SIA-003 |
| **Priority** | P1 |
| **Owner** | Build Agent + Human Review |

---

## Summary

Execute the Build Agent to implement SIA from the generated specification. This creates the agent that will learn from future build executions.

---

## Requirements

1. **Execute Build**:
   - Run Build Agent with SIA spec
   - Monitor for issues
   - Answer any questions

2. **Implementation Files**:
   - `agents/sia/execution-analyzer.ts`
   - `agents/sia/gotcha-extractor.ts`
   - `agents/sia/pattern-extractor.ts`
   - `agents/sia/knowledge-writer.ts`
   - `agents/sia/claude-md-updater.ts`

3. **Quality Gates**:
   - TypeScript compiles
   - Unit tests pass
   - Integration tests pass

---

## Pass Criteria

**PASS** when ALL of the following are true:

| # | Criterion | How to Verify |
|---|-----------|---------------|
| 1 | All tasks complete | Check statuses |
| 2 | Code compiles | `npx tsc --noEmit` |
| 3 | Tests pass | `npm test -- sia` |
| 4 | Human approved | Code review |

---

## Output Files

```
agents/sia/
├── execution-analyzer.ts
├── gotcha-extractor.ts
├── pattern-extractor.ts
├── duplicate-detector.ts
├── knowledge-writer.ts
├── confidence-tracker.ts
├── claude-md-updater.ts
├── extraction-rules.ts
└── db.ts
server/routes/sia.ts
types/sia.ts
database/migrations/031_sia.sql
tests/sia.test.ts
```
