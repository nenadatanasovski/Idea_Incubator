# UXA-005: Accessibility Checker

---

## Metadata

| Field | Value |
|-------|-------|
| **Phase** | 10 - UX Agent |
| **Depends On** | UXA-003 |
| **Blocks** | None |
| **Priority** | P2 |
| **Owner** | Human + Claude Code |

---

## Summary

Implement accessibility checking using axe-core. Validates pages against WCAG guidelines and reports violations.

---

## Context

### WCAG Levels

| Level | Description |
|-------|-------------|
| **A** | Essential accessibility |
| **AA** | Standard compliance |
| **AAA** | Enhanced accessibility |

### Common Issues

| Issue | Severity |
|-------|----------|
| Missing alt text | Serious |
| Low contrast | Moderate |
| Missing labels | Critical |
| Keyboard navigation | Serious |

---

## Requirements

1. **axe-core Integration**:
   - Inject axe-core into page
   - Run accessibility scan
   - Parse results

2. **Violation Handling**:
   - Categorize by severity
   - Include element context
   - Suggest fixes

3. **Reporting**:
   - WCAG compliance score
   - Violation list
   - Affected elements

---

## Pass Criteria

| # | Criterion | How to Verify |
|---|-----------|---------------|
| 1 | axe-core runs | Test on page |
| 2 | Violations detected | Test with bad page |
| 3 | Report generated | Check output format |

---

## Output Files

```
agents/ux/accessibility-checker.ts
```

---

## Code Template

```typescript
interface AccessibilityResult {
  passed: boolean;
  score: number;  // 0-100
  violations: AccessibilityViolation[];
  passes: number;
  incomplete: number;
}

interface AccessibilityViolation {
  id: string;
  impact: 'critical' | 'serious' | 'moderate' | 'minor';
  description: string;
  help: string;
  helpUrl: string;
  nodes: Array<{
    html: string;
    target: string[];
    failureSummary: string;
  }>;
}

export async function checkAccessibility(
  page: MCPBridge,
  options?: { level: 'A' | 'AA' | 'AAA' }
): Promise<AccessibilityResult>;
```
